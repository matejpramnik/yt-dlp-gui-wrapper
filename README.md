yt-dlp gui wrapper written in Python for downloading in multiple formats.

Embeds metadata automatically, using AudioDB free API. Optionally, you can overwrite chosen metadata entries.

Requires FFmpeg.